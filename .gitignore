.last_pull
.search_cache.json
.config.json
.download_receipts.json
__pycache__/
*.pyc

# Generated by setup.sh on every install (see setup.sh). Not tracked, so a
# locally edited/deleted launcher never counts as a change that blocks update.
run.sh
Anonrode.bat
